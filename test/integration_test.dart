import 'package:flutter_test/flutter_test.dart';

// This placeholder file remains to keep unit/widget tests under test/.
// Integration tests have been moved to integration_test/ and should be run with:
//   flutter test integration_test
// or with device-specific runners (Android instrumentation / XCTest).
void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
